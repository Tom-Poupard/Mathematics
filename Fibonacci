n = int(input("Enter the number of terms: "))

def fibonacci(n): 
    if(n <= 1):
        return n
    else:
        return (fibonacci(n-1) + fibonacci(n-2))


print(f'Fibonacci numbers up to the {n}th one :')
for i in range(n): #range(n) = range(0,n)
    print(fibonacci(i))
