n = int(input('Enter an integer number: ')) #We need 'int' so as to convert 'n' from a string to an integer
f = 1

for i in range(1, n+1): #We need to put n+1 because the range() function does not include the stop parameter
    f *= i              #it is the same as f = f*i

print(f'The factorial of {n} is: {f}')
